const showRepo = (state='yashmunjal',action)=>{
    switch(action.type){
        case 'CLICKED':
            return state;
        default :
            return state;
    }
}

export default showRepo;